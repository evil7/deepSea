# 关闭 pnpm 11 的「自动管理 packageManager 版本」注入：
# 根 package.json 的 packageManager 字段会让 pnpm 向 npm 子进程注入
# npm_config_manage_package_manager_versions（npm 不识别 → 每次打包/发布都警告）。
# 本机 pnpm 版本与 packageManager 声明一致，关闭无副作用。
manage-package-manager-versions=false
