{
  "name": "@deepsea/worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "secret:list": "wrangler secret list",
    "secret:put": "wrangler secret put",
    "secret:del": "wrangler secret delete",
    "typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.3",
    "vitest": "^3.2.7",
    "wrangler": "^4.127.0"
  }
}
