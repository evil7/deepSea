# deepSea 前端本地开发环境变量（复制为 .env 填写）
#
# 注意：GitHub OAuth 的 client_id / client_secret / redirect_uri 全部由
# apps/worker 端处理（.dev.vars / wrangler secret），前端不再持有任何 OAuth 配置。
# 前端登录按钮只跳站内 /auth/login（vite 代理 → worker）。
# 此文件可留空。