# deepSea 前端生产环境变量（vite build 优先加载，覆盖 .env）
#
# 注意：GitHub OAuth 的 client_id / client_secret / redirect_uri 全部由
# apps/worker 端处理（wrangler secret / wrangler.toml [vars]），前端不持有。
# 此文件可留空（占位：生产构建无 OAuth 相关 VITE_* 注入）。